---
title: RBAC 完整实现部署方案
version: v1.0
status: active
---

# RBAC 完整实现部署方案

## 一、代码状态（已完成，待部署）

### 后端（已完成，需修复 1 个关键 bug）

**已完成的代码修改：**

| 文件 | 状态 | 说明 |
|------|------|------|
| SysUserController.java | ✅ | 6 个 @SaCheckPermission 注解 |
| SysRoleController.java | ✅ | 5 个 @SaCheckPermission 注解 |
| SysMenuController.java | ✅ | 6 个 @SaCheckPermission 注解 |
| SysRoleServiceImpl.java | ✅ | SQL 注入修复、roleVersion 提交后递增 |
| ISysRoleService.java | ✅ | 新增 incrementRoleVersionAfterCommit 方法 |
| V1__init_rbac_tables.sql | ✅ | 5 张 RBAC 表 + 20 个索引 |
| V2__seed_roles_menus.sql | ✅ | 3 角色 + 31 菜单 + 超管权限分配 |
| AdminInitializer.java | ❌ | 需修复：CommandLineRunner 应改为 ApplicationRunner |

**待修复：AdminInitializer 启动失败**

```
错误：ERROR: relation "sys_user" does not exist
原因：AdminInitializer 使用 CommandLineRunner，执行时 Flyway 尚未迁移完成
修复：改为 ApplicationRunner + 增加 SysMenuService 依赖查询
```

**具体修改：**

```
文件：system/src/main/java/com/digital/employee/system/initializer/AdminInitializer.java

1. CommandLineRunner → ApplicationRunner
   - import org.springframework.boot.ApplicationRunner
   - import org.springframework.boot.ApplicationArguments

2. 方法签名变更
   - run(String... args) → run(ApplicationArguments args)

3. 增加 SysMenuService 依赖（构建完整权限树）
```

### 前端（已完成，待部署）

| 文件 | 状态 | 说明 |
|------|------|------|
| system-api.ts | ✅ | 16 个 CRUD 接口（用户、角色、菜单） |
| use-table.ts | ✅ | 表格分页、搜索、加载 composable |
| use-auth.ts | ✅ | 权限校验 composable |
| UserManage.vue | ✅ | 用户管理 CRUD 页面 |
| RoleManage.vue | ✅ | 角色管理 CRUD 页面 |
| v-hasPermi 指令 | ✅ | 空绑定修复 |

### 测试（已通过）

| 模块 | 测试数 | 状态 |
|------|--------|------|
| common | 11 | ✅ 全部通过 |
| system | 89 | ✅ 全部通过 |
| business | 18 | ✅ 全部通过 |
| app | 34 | ✅ 全部通过 |
| **总计** | **152** | ✅ |

---

## 二、中间件信息

### 远程 PostgreSQL

```
地址：106.54.60.80:15432
数据库：db_data_java
用户：db_data_java
密码：Z5LkrddZCsBjitht
权限：public schema 完全控制（CREATE/DROP/ALTER + 所有表）
Schema：public
```

### 远程 Redis

```
地址：106.54.60.80:16379
密码：Z5LkrddZCsBjitht
数据库：0
用途：Sa-Token 会话存储 + 角色版本缓存
```

---

## 三、部署步骤

### Phase 1：修复代码 bug（本地）

```
步骤 1.1：修复 AdminInitializer
  文件：system/src/main/java/com/digital/employee/system/initializer/AdminInitializer.java
  修改：CommandLineRunner → ApplicationRunner
  新增：SysMenuService 依赖

步骤 1.2：编译打包
  命令：mvn clean package -DskipTests

步骤 1.3：运行单元测试
  命令：mvn test -pl system
```

### Phase 2：数据库迁移（远程）

```
步骤 2.1：验证数据库连接
  用户：db_data_java
  密码：Z5LkrddZCsBjitht
  命令：psql -h 106.54.60.80 -p 15432 -U db_data_java -d db_data_java

步骤 2.2：Flyway 自动迁移
  说明：应用启动时自动执行 V1/V2 SQL
  V1：创建 sys_role, sys_user, sys_menu, sys_role_menu, sys_audit_log
  V2：插入 3 个角色 + 31 个菜单 + 超管权限

步骤 2.3：验证迁移结果
  预期：5 张表 + 3 个角色 + 31 个菜单
```

### Phase 3：后端部署（远程）

```
步骤 3.1：配置 application-remote.yml
  spring:
    datasource:
      url: jdbc:postgresql://106.54.60.80:15432/db_data_java
      username: db_data_java
      password: Z5LkrddZCsBjitht
    data:
      redis:
        host: 106.54.60.80
        port: 16379
        password: Z5LkrddZCsBjitht
  initial:
    admin:
      username: admin
      password: admin123

步骤 3.2：启动后端
  命令：SPRING_PROFILES_ACTIVE=remote java -jar app-1.0.0-SNAPSHOT.jar

步骤 3.3：验证启动
  检查：Flyway 迁移日志、AdminInitializer 管理员创建
  预期：日志显示 "管理员账号 'admin' 初始化成功"
```

### Phase 4：API 端到端测试

```
步骤 4.1：登录测试
  POST http://106.54.60.80:8080/auth/login
  Body: {"username": "admin", "password": "admin123"}
  预期：返回 token

步骤 4.2：权限校验测试
  GET http://106.54.60.80:8080/auth/me
  Header: Authorization: Bearer {token}
  预期：返回用户信息 + 权限列表

步骤 4.3：用户 CRUD 测试
  GET /api/system/user/list?page=1&size=10
  POST /api/system/user
  PUT /api/system/user/{id}
  DELETE /api/system/user/{id}

步骤 4.4：角色 CRUD 测试
  GET /api/system/role/list
  POST /api/system/role
  PUT /api/system/role/{id}
  DELETE /api/system/role/{id}

步骤 4.5：菜单测试
  GET /api/system/menu/tree
  预期：返回完整菜单树
```

### Phase 5：前端部署

```
步骤 5.1：构建前端
  命令：cd digital-employee-web && npm run build
  输出：dist/

步骤 5.2：部署到 Web 服务器
  方式：Nginx 或直接部署到静态文件服务
  配置：API 代理到 http://106.54.60.80:8080

步骤 5.3：验证完整登录流程
  1. 访问前端页面
  2. 使用 admin/admin123 登录
  3. 验证侧边栏菜单显示
  4. 验证用户管理页面
  5. 验证角色管理页面
```

---

## 四、验证清单

### 后端验证

- [ ] Flyway 迁移成功（5 张表创建）
- [ ] 管理员账号初始化成功
- [ ] 登录接口正常返回 token
- [ ] /auth/me 返回权限列表
- [ ] 用户 CRUD 四个接口正常
- [ ] 角色 CRUD 四个接口正常
- [ ] 菜单树接口正常
- [ ] Sa-Token Redis 会话存储正常
- [ ] 角色版本缓存正常

### 前端验证

- [ ] 登录页面正常显示
- [ ] 登录后跳转到首页
- [ ] 侧边栏菜单显示正确
- [ ] 用户管理页面加载正常
- [ ] 角色管理页面加载正常
- [ ] 权限控制生效（无权限按钮隐藏）
- [ ] CRUD 操作正常（新增、编辑、删除）
- [ ] 分页、搜索功能正常

---

## 五、问题排查

### 问题 1：AdminInitializer 启动失败

```
错误：ERROR: relation "sys_user" does not exist
原因：CommandLineRunner 执行时 Flyway 尚未完成
修复：改为 ApplicationRunner
```

### 问题 2：数据库权限不足

```
错误：permission denied for schema public
原因：db_data_java 用户权限未授予
修复：GRANT ALL ON SCHEMA public TO db_data_java;
      GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA public TO db_data_java;
```

### 问题 3：Flyway 迁移历史冲突

```
错误：FlywayException: Failed to check whether schema public is
     under Flyway control
原因：之前手动执行过 SQL，残留 flyway_schema_history
修复：DELETE FROM flyway_schema_history;
      或 DROP TABLE flyway_schema_history;
```

---

## 六、生产环境建议

### 1. 安全加固

```
- Redis 密码使用环境变量注入
- 数据库密码使用密钥管理服务
- 启用 HTTPS
- 配置 CORS 白名单
```

### 2. 性能优化

```
- HikariCP 连接池调优
- Redis 连接池配置
- Flyway 迁移脚本版本管理
```

### 3. 监控告警

```
- 应用健康检查（/actuator/health）
- 数据库连接监控
- Redis 连接监控
- 慢查询告警
```

---

## 七、后续工作

| 阶段 | 任务 | 优先级 |
|------|------|--------|
| P0 | 修复 AdminInitializer bug | 高 |
| P0 | 执行数据库迁移 | 高 |
| P0 | 部署后端到远程服务器 | 高 |
| P0 | API 端到端测试 | 高 |
| P1 | 部署前端 | 中 |
| P1 | 完整登录流程验证 | 中 |
| P2 | 生产环境安全加固 | 低 |
| P2 | 性能调优 | 低 |

---

## 附录 A：数据库迁移脚本

```sql
-- V1__init_rbac_tables.sql（执行顺序 1）
CREATE TABLE sys_role (
    id BIGSERIAL PRIMARY KEY,
    role_key VARCHAR(64) NOT NULL UNIQUE,
    role_name VARCHAR(64) NOT NULL,
    status SMALLINT NOT NULL DEFAULT 1,
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE sys_user (
    id BIGSERIAL PRIMARY KEY,
    username VARCHAR(64) NOT NULL UNIQUE,
    password_hash VARCHAR(255) NOT NULL,
    nickname VARCHAR(64),
    phone VARCHAR(20) UNIQUE,
    role_id BIGINT NOT NULL REFERENCES sys_role(id),
    status SMALLINT NOT NULL DEFAULT 1,
    must_change_password BOOLEAN NOT NULL DEFAULT FALSE,
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE sys_menu (
    id BIGSERIAL PRIMARY KEY,
    parent_id BIGINT NOT NULL DEFAULT 0,
    title VARCHAR(64) NOT NULL,
    menu_type CHAR(1) NOT NULL,
    path VARCHAR(128),
    component VARCHAR(128),
    perms VARCHAR(128),
    icon VARCHAR(64),
    sort_order INT NOT NULL DEFAULT 0,
    visible SMALLINT NOT NULL DEFAULT 1,
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE sys_role_menu (
    role_id BIGINT NOT NULL REFERENCES sys_role(id) ON DELETE CASCADE,
    menu_id BIGINT NOT NULL REFERENCES sys_menu(id) ON DELETE CASCADE,
    PRIMARY KEY (role_id, menu_id)
);

CREATE TABLE sys_audit_log (
    id BIGSERIAL PRIMARY KEY,
    user_id BIGINT,
    username VARCHAR(64),
    ip VARCHAR(64),
    module VARCHAR(64) NOT NULL,
    action VARCHAR(64) NOT NULL,
    target_id VARCHAR(64),
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);
```

---

## 附录 B：配置文件示例

```yaml
# application-remote.yml
spring:
  datasource:
    url: jdbc:postgresql://106.54.60.80:15432/db_data_java
    username: db_data_java
    password: Z5LkrddZCsBjitht
  data:
    redis:
      host: 106.54.60.80
      port: 16379
      password: Z5LkrddZCsBjitht

initial:
  admin:
    username: admin
    password: admin123
```

---

## 附录 C：API 接口清单

### 认证接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /auth/login | 用户登录 |
| POST | /auth/logout | 退出登录 |
| GET | /auth/me | 获取当前用户信息 |

### 用户管理接口

| 方法 | 路径 | 权限 |
|------|------|------|
| GET | /api/system/user/list | admin:user:readonly |
| POST | /api/system/user | admin:user:manage |
| PUT | /api/system/user/{id} | admin:user:manage |
| DELETE | /api/system/user/{id} | admin:user:manage |

### 角色管理接口

| 方法 | 路径 | 权限 |
|------|------|------|
| GET | /api/system/role/list | admin:permission:readonly |
| POST | /api/system/role | admin:permission:manage |
| PUT | /api/system/role/{id} | admin:permission:manage |
| DELETE | /api/system/role/{id} | admin:permission:manage |

### 菜单管理接口

| 方法 | 路径 | 权限 |
|------|------|------|
| GET | /api/system/menu/tree | admin:menu:readonly |
| POST | /api/system/menu | admin:menu:manage |
| PUT | /api/system/menu/{id} | admin:menu:manage |
| DELETE | /api/system/menu/{id} | admin:menu:manage |

---

## 附录 D：部署命令参考

```bash
# 编译打包
mvn clean package -DskipTests

# 运行测试
mvn test

# 启动后端（远程环境）
SPRING_PROFILES_ACTIVE=remote \
java -jar app/target/app-1.0.0-SNAPSHOT.jar

# 构建前端
cd digital-employee-web
npm install
npm run build

# 数据库迁移（首次）
psql -h 106.54.60.80 -p 15432 -U db_data_java -d db_data_java \
  -f app/src/main/resources/db/migration/V1__init_rbac_tables.sql

psql -h 106.54.60.80 -p 15432 -U db_data_java -d db_data_java \
  -f app/src/main/resources/db/migration/V2__seed_roles_menus.sql
```

---

**文档版本**：v1.0  
**创建时间**：2026-09-08  
**最后更新**：2026-09-08  
**维护者**：Claude Code
